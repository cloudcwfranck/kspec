# GitHub API Configuration
# Optional: Provides higher rate limits (5000 req/hr vs 60 req/hr)
# Create a token at: https://github.com/settings/tokens
# Required scopes: public_repo (read-only)
GITHUB_TOKEN=

# GitHub Repository
# Defaults shown below - override if needed
GITHUB_OWNER=cloudcwfranck
GITHUB_REPO=kspec
