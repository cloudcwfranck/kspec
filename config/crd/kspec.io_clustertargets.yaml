---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: clustertargets.kspec.io
spec:
  group: kspec.io
  names:
    kind: ClusterTarget
    listKind: ClusterTargetList
    plural: clustertargets
    shortNames:
    - ct
    singular: clustertarget
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.apiServerURL
      name: API Server
      type: string
    - jsonPath: .status.reachable
      name: Reachable
      type: boolean
    - jsonPath: .status.version
      name: Version
      type: string
    - jsonPath: .status.platform
      name: Platform
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterTarget defines a remote Kubernetes cluster that can be
          scanned by the operator
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterTargetSpec defines the desired state of ClusterTarget
            properties:
              allowEnforcement:
                default: false
                description: |-
                  AllowEnforcement enables policy enforcement and drift remediation on this cluster
                  If false (default), the cluster will be scanned read-only
                type: boolean
              apiServerURL:
                description: APIServerURL is the Kubernetes API server endpoint (e.g.,
                  https://k8s.example.com:6443)
                pattern: ^https?://.*
                type: string
              authMode:
                description: AuthMode specifies the authentication method to use
                enum:
                - kubeconfig
                - serviceAccount
                - token
                type: string
              caData:
                description: |-
                  CAData contains PEM-encoded certificate authority certificates
                  If specified, used to verify the cluster's API server certificate
                format: byte
                type: string
              insecureSkipTLSVerify:
                description: |-
                  InsecureSkipTLSVerify skips the validity check for the server's certificate
                  This will make your connection insecure and should only be used for testing
                type: boolean
              kubeconfigSecretRef:
                description: |-
                  KubeconfigSecretRef references a Secret containing a kubeconfig file
                  Required when authMode is "kubeconfig"
                properties:
                  key:
                    description: |-
                      Key is the key within the secret data
                      Defaults to "kubeconfig" for kubeconfig mode, "token" for token/serviceAccount modes
                    type: string
                  name:
                    description: Name is the name of the secret
                    type: string
                  namespace:
                    description: |-
                      Namespace is the namespace of the secret
                      If not specified, uses the same namespace as the ClusterTarget
                    type: string
                required:
                - name
                type: object
              proxyURL:
                description: ProxyURL is the URL of the proxy to use for this cluster
                  connection
                type: string
              scanInterval:
                description: |-
                  ScanInterval specifies how often to scan this cluster
                  If not specified, uses the default reconciliation interval
                type: string
              serviceAccountSecretRef:
                description: |-
                  ServiceAccountSecretRef references a Secret containing ServiceAccount token
                  Required when authMode is "serviceAccount"
                properties:
                  key:
                    description: |-
                      Key is the key within the secret data
                      Defaults to "kubeconfig" for kubeconfig mode, "token" for token/serviceAccount modes
                    type: string
                  name:
                    description: Name is the name of the secret
                    type: string
                  namespace:
                    description: |-
                      Namespace is the namespace of the secret
                      If not specified, uses the same namespace as the ClusterTarget
                    type: string
                required:
                - name
                type: object
              tokenSecretRef:
                description: |-
                  TokenSecretRef references a Secret containing a bearer token
                  Required when authMode is "token"
                properties:
                  key:
                    description: |-
                      Key is the key within the secret data
                      Defaults to "kubeconfig" for kubeconfig mode, "token" for token/serviceAccount modes
                    type: string
                  name:
                    description: Name is the name of the secret
                    type: string
                  namespace:
                    description: |-
                      Namespace is the namespace of the secret
                      If not specified, uses the same namespace as the ClusterTarget
                    type: string
                required:
                - name
                type: object
            required:
            - apiServerURL
            - authMode
            type: object
          status:
            description: ClusterTargetStatus defines the observed state of ClusterTarget
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the ClusterTarget's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastChecked:
                description: LastChecked is the timestamp of the last health check
                format: date-time
                type: string
              nodeCount:
                description: NodeCount is the number of nodes in the cluster
                format: int32
                type: integer
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed ClusterTarget
                format: int64
                type: integer
              platform:
                description: Platform describes the cluster platform (e.g., "eks",
                  "gke", "aks", "openshift", "vanilla")
                type: string
              reachable:
                description: Reachable indicates whether the cluster is currently
                  reachable
                type: boolean
              uid:
                description: UID is the unique identifier of the cluster
                type: string
              version:
                description: Version is the Kubernetes version of the cluster
                type: string
            required:
            - reachable
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
